<Window x:Class="AryoVideoPlayer.LanguageSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="انتخاب زبان ترجمه"
        Height="300" Width="350"
        Background="#2D2D2D"
        Foreground="White"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Window.Resources>
        <Style x:Key="LangButton" TargetType="Button">
            <Setter Property="Background" Value="#3D3D3D"/>
            <Setter Property="Foreground" Value="#C0C0C0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#6C63FF"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- هدر -->
        <TextBlock Grid.Row="0" 
                   Text="ترجمه زیرنویس" 
                   FontSize="20" FontWeight="Bold"
                   Foreground="White"
                   Margin="0,0,0,15"/>

        <!-- زبان مبدأ -->
        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="زبان مبدأ:" Foreground="#999999" Margin="0,0,0,5"/>
            <ComboBox x:Name="SourceLangComboBox" 
                       Background="#3D3D3D" 
                       Foreground="White"
                       SelectedIndex="1">
                <ComboBoxItem Content="فارسی (fa)"/>
                <ComboBoxItem Content="انگلیسی (en)"/>
                <ComboBoxItem Content="فرانسوی (fr)"/>
                <ComboBoxItem Content="آلمانی (de)"/>
                <ComboBoxItem Content="اسپانیایی (es)"/>
                <ComboBoxItem Content="ایتالیایی (it)"/>
                <ComboBoxItem Content="ژاپنی (ja)"/>
                <ComboBoxItem Content="کره‌ای (ko)"/>
                <ComboBoxItem Content="چینی (zh)"/>
                <ComboBoxItem Content="عربی (ar)"/>
                <ComboBoxItem Content="ترکی (tr)"/>
                <ComboBoxItem Content="روسی (ru)"/>
            </ComboBox>
        </StackPanel>

        <!-- زبان مقصد -->
        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="زبان مقصد:" Foreground="#999999" Margin="0,0,0,5"/>
            <ComboBox x:Name="TargetLangComboBox" 
                      Background="#3D3D3D" 
                      Foreground="White"
                      SelectedIndex="0">
                <ComboBoxItem Content="فارسی (fa)"/>
                <ComboBoxItem Content="انگلیسی (en)"/>
                <ComboBoxItem Content="فرانسوی (fr)"/>
                <ComboBoxItem Content="آلمانی (de)"/>
                <ComboBoxItem Content="اسپانیایی (es)"/>
                <ComboBoxItem Content="ایتالیایی (it)"/>
                <ComboBoxItem Content="ژاپنی (ja)"/>
                <ComboBoxItem Content="کره‌ای (ko)"/>
                <ComboBoxItem Content="چینی (zh)"/>
                <ComboBoxItem Content="عربی (ar)"/>
                <ComboBoxItem Content="ترکی (tr)"/>
                <ComboBoxItem Content="روسی (ru)"/>
            </ComboBox>
        </StackPanel>

        <!-- دکمه‌ها -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="لغو" 
                    Style="{StaticResource LangButton}"
                    Click="Cancel_Click"
                    Width="80"/>
            <Button Content="ترجمه" 
                    Style="{StaticResource LangButton}"
                    Click="Translate_Click"
                    Width="80"
                    Background="#6C63FF"
                    Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>
